<Page x:Class="AdvancedChat.Desktop.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Login">
    <Grid>
        <materialDesign:Card Width="350" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center" materialDesign:ElevationAssist.Elevation="Dp4" UniformCornerRadius="16">
            <StackPanel>
                <materialDesign:PackIcon Kind="ChatOutline" Width="64" Height="64" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,16"/>
                <TextBlock Text="Advanced Chat" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" Margin="0,0,0,32" FontWeight="Bold"/>

                <TextBox x:Name="EmailBox" materialDesign:HintAssist.Hint="Email" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" FontSize="16"/>
                
                <PasswordBox x:Name="PasswordBox" materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,32" FontSize="16"/>

                <Button Content="LOGIN" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="8" Height="45" FontSize="16" Click="Login_Click" Margin="0,0,0,16"/>

                <TextBlock x:Name="ErrorText" Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" TextAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Page>
